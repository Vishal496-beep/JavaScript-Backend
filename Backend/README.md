# Always make sure you have created a (.env) file for PORT otherwise it will crash it wont run

# /api/jpkes ~ that should be a better choice for api nd all 
# CORS means Cross Origin Request \ means the port url everything must be same otherwise it will a error || we gotta ip whitelist otherwise

# we can use proxy for the server in config 